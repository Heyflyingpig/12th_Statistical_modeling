# Q1 Archive

这里存放 Q1 的历史方法文件，不作为当前年度版正式流程的入口。

当前约定：

1. `legacy_method_notebooks/` 保存旧季度口径 notebook、临时转换 notebook 等历史实现。
2. 根目录中的 `.py` 脚本才是当前年度版正式方法。

如果需要追溯旧思路，可以查看本目录；如果需要继续比赛建模，请优先使用 `Q1/` 根目录中的年度版脚本。
